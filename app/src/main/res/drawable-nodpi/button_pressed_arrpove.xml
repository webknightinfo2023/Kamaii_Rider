<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <solid android:color="@color/approve" />
    <stroke
        android:width="1px"
        android:color="@color/approve" />
    <!-- Round Corners -->
    <corners android:radius="8dp"></corners>
</shape>
