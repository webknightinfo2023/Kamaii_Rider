<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <solid android:color="@color/customercolor" />
    <stroke
        android:width="1px"
        android:color="@color/customercolor" />
    <!-- Round Corners -->
    <corners android:radius="20dp"></corners>
</shape>
